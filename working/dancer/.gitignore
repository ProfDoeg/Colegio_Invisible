# Generated artifacts — large, exploratory, regenerable from the scripts.
# The pipeline (the .py scripts above) is committed; the outputs are not.
refs/
actions/
processed/
animations/
sora/
contact_sheet.png
