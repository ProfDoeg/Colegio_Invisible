[pytest]
testpaths = tests
addopts = -q
markers =
    rpc: needs a reachable Dogecoin node (opt in with QUIPU_RPC_TESTS=1)
    slow: long-running (broadcast simulations, large builds)
    latex: needs xelatex on PATH
